import { auth, db, storage } from './config';

export { auth, db, storage };

// You can add additional Firebase-related functions here as your app grows
// For example:
// export const createUser = (email, password) => { ... }
// export const uploadFile = (file) => { ... }
