[build]
  command = "npm run build:netlify"
  publish = "dist"

[build.environment]
  SHARP_IGNORE_GLOBAL_LIBVIPS = "1"
  DISABLE_SHARP_INSTALLATION = "true" 
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
